spring:
  main:
    cloud-platform: kubernetes
  cloud:
    kubernetes:
      discovery:
        metadata:
          add-labels: true
          add-pod-labels: true
        namespaces:
          - app
      client:
        namespace: app