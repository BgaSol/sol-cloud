server:
  port: 8082
  servlet:
    context-path: /${spring.application.name}
system:
  describe: 系统文件服务，服务内对接的minio，向微服务内部暴露的api接口。
spring:
  profiles:
    active:
      - web
      - database
      - discovery
      - loadbalancer
      - logger
      - micrometer
      - minio
      - feign
      - pg-m1
      - redis
      - sa-token
      - swagger
